export const EMAIL_TOKEN_LENGTH = 16;

export const HEADERS = {
	DEVICE_ID: "x-device-id",
	POSTMARK_TOKEN: "x-postmark-token",
} as const;

export const SOURCE_TYPE = {
	SYNCED: "synced",
} as const;
